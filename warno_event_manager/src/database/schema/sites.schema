CREATE TABLE "public"."sites" (
    "site_id" serial,
    "name_short" VARCHAR(8) NOT NULL,
    "name_long" VARCHAR(128),
    "latitude" float,
    "longitude" float,
    "facility" VARCHAR(8),
    "mobile" BOOLEAN,
    "location_name" VARCHAR(64),
    PRIMARY KEY ("site_id")
);
